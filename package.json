{
    "private": true,
    "scripts": {
        "build": "webpack -p",
        "build:debug": "ng build && npm run copy-assets",
        "build:release": "ng build --prod && npm run copy-assets",
        "pack": "true",
        "publish": "semantic-release",
        "test": "echo ng test",
    },
    "devDependencies": {
        "semantic-release": "^18.0.0",
        "webpack": "^5.58.0",
        "webpack-cli": "^4.9.0",
        "webpack-dev-server": "^4.3.1",
        "@semantic-release/changelog": "^6.0.0",
        "@semantic-release/exec": "^6.0.1",
        "@semantic-release/git": "^10.0.0",
        "@semantic-release/gitlab": "^7.0.3",
        "semantic-release": "^18.0.0"
    },
    "dependencies": {
    }
}
